{% for node in pages_list %}
  {% if group == null or group == node.group %}
    {% if page.url == node.url %}
      <li role="presentation" class="active"><a href="{{ node.url | prepend: site.baseurl }}">{{ node.title }}</a></li>
	{% elsif node.draft == true %}
    {% else %}
      <li role="presentation"><a href="{{ node.url | prepend: site.baseurl }}">{{ node.title }}</a></li>
    {% endif %}
  {% endif %}
{% endfor %}
{% assign pages_list = nil %}
{% assign group = nil %}
